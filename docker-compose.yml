services:
  db:
    image: docker.io/postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: nicolas
      POSTGRES_PASSWORD: secretpassword
      POSTGRES_DB: dex
